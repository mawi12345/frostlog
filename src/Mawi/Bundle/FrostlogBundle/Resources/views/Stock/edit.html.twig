<div data-role="page"> 
    <div data-role="header">
		<a href="{{ path('home') }}" data-icon="arrow-l" data-direction="reverse">abbrechen</a>
		<h1>Bearbeiten</h1>
	</div>
	<div data-role="content">
		<form action="{{ path('stock_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
		    <input type="hidden" name="_method" value="PUT" />
            {{ form_widget(edit_form) }}
		    <p>
		        <button type="submit" data-direction="reverse" data-icon="check">speichern</button>
		    </p>
		</form>
        <hr>
        <form action="{{ path('stock_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit" data-icon="delete" data-theme="e">löschen</button>
        </form>
	</div>
</div>
